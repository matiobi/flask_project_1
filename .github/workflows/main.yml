name: Main
on:
  push:
    branches:
    - main
    - production
  pull_request:
    branches:
    - main
    - production 
  workflow_dispatch:
jobs: 
  web-deploy:
    name: Wdrożenie na serwer
    runs-on: ubuntu-latest
    steps:
    - name: Akcja GITHUB
      uses: actions/chceckout@v4

    - name: Synchronizacja do FTP
      uses: SamKirland/FTP-Deploy-Action@v4.3.4
      with:
        server: 74.235.157.147
        username: ${{secrets.ftp_username}}
        password: ${{secrets.ftp_password}}
  
    
